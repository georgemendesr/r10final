#############################################################
# EXEMPLO DE CONFIGURAÇÃO (.env.example)
# Copie para .env e preencha os valores reais.
# NUNCA versione o arquivo .env com segredos.
#############################################################

# URL pública onde a Meta acessa as imagens geradas (sem barra final)
PUBLIC_BASE_URL=https://seu-dominio.com

# Instagram / Facebook Graph (permissões mínimas: instagram_basic, instagram_manage_insights, pages_read_engagement, pages_read_user_content)
IG_BUSINESS_ID=
FB_PAGE_ID=
IG_ACCESS_TOKEN=
# Alternativas (use somente se preferir nomenclatura diferente)
# FB_ACCESS_TOKEN=
# META_ACCESS_TOKEN=
# Se você já tem um Page Access Token válido e estável (recomendado para produção), defina:
# FB_PAGE_ACCESS_TOKEN=

# IA (Groq) — opcional
GROQ_API_KEY=
GROQ_MODEL=llama3-8b-8192

# Persistência (opcional)
# PERSIST_DIR=C:\\data\\instagram-publisher

# Banco SQLite customizado (normalmente não precisa alterar)
# SQLITE_DB_PATH=c:\\Users\\George Mendes\\Desktop\\r10final\\server\\noticias.db

# Porta do backend principal (API)
PORT=3002

# Configuração para KingHost
# Este arquivo indica que o projeto está otimizado para deploy na KingHost

# Arquivo de inicialização
STARTUP_FILE=server/server-api-simple.cjs

# Versão Node recomendada
NODE_VERSION=18

# Modo de execução
NODE_ENV=production

# Build do frontend necessário
BUILD_FRONTEND=true
BUILD_COMMAND=cd r10-front_full_07ago && npm install && npm run build && cd .. && cp -r r10-front_full_07ago/dist ./dist

# Variáveis obrigatórias (configurar no painel)
REQUIRED_ENV_VARS=NODE_ENV,PORT,JWT_SECRET,SERVE_STATIC_FRONT

# Variáveis opcionais
OPTIONAL_ENV_VARS=GROQ_API_KEY,INSTAGRAM_ACCESS_TOKEN,PUBLIC_BASE_URL

<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <style>
    :root {
      --primary-color: #FF6B35;
      --secondary-color: #1a1a2e;
      --accent-color: #4ECDC4;
      --bg-gradient-start: #f8f9fa;
      --bg-gradient-end: #e9ecef;
      --card-shadow: 0 4px 6px rgba(0, 0, 0, 0.07);
      --card-shadow-hover: 0 8px 16px rgba(0, 0, 0, 0.12);
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, var(--bg-gradient-start) 0%, var(--bg-gradient-end) 100%);
      min-height: 100vh;
      padding-bottom: 3rem;
    }

    .header-arquivo {
      background: linear-gradient(135deg, #2d3748 0%, #1a202c 100%);
      box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
      border-bottom: 3px solid var(--primary-color);
      padding: 1rem 0;
      margin-bottom: 2rem;
    }

    .header-arquivo .logo-r10 {
      height: 80px;
      width: auto;
      transition: transform 0.3s ease;
    }

    .header-arquivo .logo-r10:hover {
      transform: scale(1.05);
    }

    .header-arquivo h1 {
      color: white;
      font-weight: 700;
      margin: 0;
      font-size: 1.5rem;
    }

    .header-arquivo .subtitle {
      color: rgba(255, 255, 255, 0.7);
      font-size: 0.9rem;
      margin-top: 0.3rem;
    }

    .card-noticia {
      background: white;
      border: none;
      border-radius: 16px;
      overflow: hidden;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      margin-bottom: 1.5rem;
      box-shadow: var(--card-shadow);
    }

    .card-noticia:hover {
      transform: translateY(-8px);
      box-shadow: var(--card-shadow-hover);
    }

    .card-noticia img {
      width: 100%;
      height: 220px;
      object-fit: cover;
      transition: transform 0.3s ease;
    }

    .card-noticia:hover img {
      transform: scale(1.05);
    }

    .card-body {
      padding: 1.5rem;
    }

    .chapeu-badge {
      background: linear-gradient(135deg, var(--primary-color) 0%, #ff8c61 100%);
      color: white;
      padding: 0.4rem 1rem;
      border-radius: 20px;
      font-size: 0.7rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.8px;
      display: inline-block;
      box-shadow: 0 2px 8px rgba(255, 107, 53, 0.3);
    }

    .meta-info {
      color: #666;
      font-size: 0.85rem;
      margin-top: 0.5rem;
    }

    .meta-info i {
      margin-right: 0.3rem;
      color: var(--primary-color);
    }

    .btn-primary {
      background: var(--primary-color);
      border: none;
      padding: 0.6rem 1.5rem;
      border-radius: 25px;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .btn-primary:hover {
      background: #a01628;
      transform: translateY(-2px);
      box-shadow: 0 4px 15px rgba(196, 30, 58, 0.3);
    }

    .pagination {
      margin-top: 2rem;
      justify-content: center;
    }

    .page-link {
      color: var(--primary-color);
      border: 1px solid var(--glass-border);
      background: rgba(255, 255, 255, 0.9);
      margin: 0 0.2rem;
      border-radius: 8px;
    }

    .page-link:hover {
      background: var(--primary-color);
      color: white;
    }

    .page-item.active .page-link {
      background: var(--primary-color);
      border-color: var(--primary-color);
    }

    .filter-section {
      background: rgba(255, 255, 255, 0.95);
      padding: 1.5rem;
      border-radius: 12px;
      margin-bottom: 2rem;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    }

    .noticia-resumo {
      color: #555;
      line-height: 1.6;
      margin: 1rem 0;
    }

    .footer-arquivo {
      background: rgba(26, 26, 46, 0.95);
      color: rgba(255, 255, 255, 0.7);
      text-align: center;
      padding: 1.5rem 0;
      margin-top: 3rem;
    }
  </style>
</head>
<body>
  
  <!-- Header -->
  <header class="header-arquivo">
    <div class="container">
      <div class="d-flex align-items-center justify-content-between">
        <div class="d-flex align-items-center gap-3">
          <img src="https://res.cloudinary.com/dlogsw1hy/image/upload/v1744569420/logo-r10-piaui_ylwqfo.png" alt="R10 Piauí" class="logo-r10">
          <div>
            <h1><i class="bi bi-archive"></i> Arquivo de Notícias</h1>
            <div class="subtitle">Notícias Históricas</div>
          </div>
        </div>
        <a href="/" class="btn btn-outline-light">
          <i class="bi bi-house"></i> Início
        </a>
      </div>
    </div>
  </header>

  <!-- Conteúdo Principal -->
  <main class="container">
    
    <!-- Painel de Estatísticas -->
    <% if (stats && stats.total_noticias) { %>
    <div class="row mb-4">
      <div class="col-md-3 col-sm-6 mb-3">
        <div class="card border-0 shadow-sm h-100" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
          <div class="card-body text-center text-white">
            <i class="bi bi-newspaper" style="font-size: 2.5rem; opacity: 0.9;"></i>
            <h2 class="mt-3 mb-0" style="font-size: 2.5rem; font-weight: 700;"><%= stats.total_noticias.toLocaleString('pt-BR') %></h2>
            <p class="mb-0 mt-2">Notícias Preservadas</p>
          </div>
        </div>
      </div>
      <div class="col-md-3 col-sm-6 mb-3">
        <div class="card border-0 shadow-sm h-100" style="background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);">
          <div class="card-body text-center text-white">
            <i class="bi bi-images" style="font-size: 2.5rem; opacity: 0.9;"></i>
            <h2 class="mt-3 mb-0" style="font-size: 2.5rem; font-weight: 700;">13.786</h2>
            <p class="mb-0 mt-2">Imagens Arquivadas</p>
          </div>
        </div>
      </div>
      <div class="col-md-3 col-sm-6 mb-3">
        <div class="card border-0 shadow-sm h-100" style="background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);">
          <div class="card-body text-center text-white">
            <i class="bi bi-calendar-range" style="font-size: 2.5rem; opacity: 0.9;"></i>
            <h2 class="mt-3 mb-0" style="font-size: 2.5rem; font-weight: 700;"><%= stats.total_dias.toLocaleString('pt-BR') %></h2>
            <p class="mb-0 mt-2">Dias de Cobertura</p>
          </div>
        </div>
      </div>
      <div class="col-md-3 col-sm-6 mb-3">
        <div class="card border-0 shadow-sm h-100" style="background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);">
          <div class="card-body text-center text-white">
            <i class="bi bi-clock-history" style="font-size: 2.5rem; opacity: 0.9;"></i>
            <h2 class="mt-3 mb-0" style="font-size: 2rem; font-weight: 700;">
              <%= new Date(stats.data_primeira).getFullYear() %> - <%= new Date(stats.data_ultima).getFullYear() %>
            </h2>
            <p class="mb-0 mt-2">Período Histórico</p>
          </div>
        </div>
      </div>
    </div>
    <% } %>
    
    <!-- Filtros -->
    <div class="filter-section">
      <form method="GET" action="/" class="row g-3">
        <div class="col-md-9">
          <label class="form-label"><i class="bi bi-search"></i> Buscar notícias</label>
          <input 
            type="text" 
            class="form-control" 
            name="busca" 
            placeholder="Digite título ou palavras-chave..."
            value="<%= filtroBusca %>"
          >
        </div>
        <div class="col-md-3 d-flex align-items-end">
          <button type="submit" class="btn btn-primary w-100">
            <i class="bi bi-search"></i> Buscar
          </button>
        </div>
      </form>
    </div>

    <!-- Informações -->
    <div class="mb-3 text-white">
      <small>
        <i class="bi bi-info-circle"></i> 
        Exibindo <%= noticias.length %> de <%= totalNoticias %> notícias
        <% if (filtroBusca) { %>
          | Busca: <strong>"<%= filtroBusca %>"</strong>
        <% } %>
      </small>
    </div>

    <!-- Grid de Notícias -->
    <div class="row">
      <% if (noticias.length === 0) { %>
        <div class="col-12">
          <div class="card-noticia text-center p-5">
            <i class="bi bi-inbox" style="font-size: 3rem; color: #ccc;"></i>
            <h4 class="mt-3">Nenhuma notícia encontrada</h4>
            <p class="text-muted">Tente alterar os filtros de busca.</p>
            <a href="/" class="btn btn-primary mt-2">Limpar filtros</a>
          </div>
        </div>
      <% } else { %>
        <% noticias.forEach(noticia => { %>
          <div class="col-md-6 col-lg-4">
            <div class="card-noticia">
              <% if (noticia.imagem_cloudinary) { %>
                <img src="<%= noticia.imagem_cloudinary %>" alt="<%= noticia.titulo %>" 
                     onerror="this.src='https://via.placeholder.com/400x200?text=Sem+Imagem'">
              <% } else { %>
                <img src="https://via.placeholder.com/400x200?text=Sem+Imagem" alt="Placeholder">
              <% } %>
              
              <div class="card-body">
                <div class="mb-2">
                  <span class="chapeu-badge"><%= noticia.categoria %></span>
                </div>
                
                <h5 class="card-title">
                  <%= noticia.titulo.substring(0, 80) %><%= noticia.titulo.length > 80 ? '...' : '' %>
                </h5>
                
                <p class="noticia-resumo">
                  <%= noticia.conteudo.replace(/<[^>]*>/g, '').substring(0, 120) %>...
                </p>
                
                <div class="meta-info mb-3">
                  <i class="bi bi-calendar3"></i> 
                  <%= new Date(noticia.data_publicacao).toLocaleDateString('pt-BR') %>
                </div>
                
                <a href="/arquivo/noticia/<%= noticia.id %>" class="btn btn-primary btn-sm w-100">
                  Ler notícia completa <i class="bi bi-arrow-right"></i>
                </a>
              </div>
            </div>
          </div>
        <% }) %>
      <% } %>
    </div>

    <!-- Paginação -->
    <% if (totalPages > 1) { %>
      <nav aria-label="Navegação de páginas">
        <ul class="pagination">
          
          <!-- Página anterior -->
          <li class="page-item <%= currentPage === 1 ? 'disabled' : '' %>">
            <a class="page-link" href="?page=<%= currentPage - 1 %><%= filtroBusca ? '&busca=' + filtroBusca : '' %>">
              <i class="bi bi-chevron-left"></i>
            </a>
          </li>

          <!-- Números de página -->
          <% for (let i = 1; i <= totalPages; i++) { %>
            <% if (i === 1 || i === totalPages || (i >= currentPage - 2 && i <= currentPage + 2)) { %>
              <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                <a class="page-link" href="?page=<%= i %><%= filtroBusca ? '&busca=' + filtroBusca : '' %>">
                  <%= i %>
                </a>
              </li>
            <% } else if (i === currentPage - 3 || i === currentPage + 3) { %>
              <li class="page-item disabled">
                <span class="page-link">...</span>
              </li>
            <% } %>
          <% } %>

          <!-- Próxima página -->
          <li class="page-item <%= currentPage === totalPages ? 'disabled' : '' %>">
            <a class="page-link" href="?page=<%= currentPage + 1 %><%= filtroBusca ? '&busca=' + filtroBusca : '' %>">
              <i class="bi bi-chevron-right"></i>
            </a>
          </li>

        </ul>
      </nav>
    <% } %>

  </main>

  <!-- Footer -->
  <footer class="footer-arquivo">
    <div class="container">
      <div class="row align-items-center py-4">
        <!-- Logo e Texto -->
        <div class="col-md-4 text-center text-md-start mb-3 mb-md-0">
          <img src="/images/logo-r10-piaui.png" alt="R10 Piauí" style="height: 60px; filter: brightness(0) invert(1);" class="mb-2">
          <p class="mb-0 text-white-50">
            <strong style="color: #FF6B35; font-size: 1.1rem;">Desde 2014</strong><br>
            <small>Arquivo de Notícias Históricas</small>
          </p>
        </div>
        
        <!-- Redes Sociais -->
        <div class="col-md-4 text-center mb-3 mb-md-0">
          <h6 class="text-white mb-3">Siga-nos nas Redes Sociais</h6>
          <div class="social-links">
            <a href="https://facebook.com/r10piaui" target="_blank" rel="noopener noreferrer" title="Facebook">
              <i class="bi bi-facebook"></i>
            </a>
            <a href="https://instagram.com/r10piaui" target="_blank" rel="noopener noreferrer" title="Instagram">
              <i class="bi bi-instagram"></i>
            </a>
            <a href="https://youtube.com/@r10piaui" target="_blank" rel="noopener noreferrer" title="YouTube">
              <i class="bi bi-youtube"></i>
            </a>
            <a href="https://tiktok.com/@r10piaui" target="_blank" rel="noopener noreferrer" title="TikTok">
              <i class="bi bi-tiktok"></i>
            </a>
          </div>
        </div>
        
        <!-- Informações -->
        <div class="col-md-4 text-center text-md-end">
          <p class="mb-0 text-white-50">
            <i class="bi bi-archive-fill"></i> Sistema de Arquivo<br>
            <small>Preservando a história do jornalismo piauiense</small>
          </p>
        </div>
      </div>
      
      <!-- Copyright -->
      <div class="row">
        <div class="col-12 text-center pt-3" style="border-top: 1px solid rgba(255,255,255,0.1);">
          <small class="text-white-50">
            © 2014-2025 R10 Piauí - Todos os direitos reservados
          </small>
        </div>
      </div>
    </div>
  </footer>

  <style>
    .footer-arquivo {
      background: linear-gradient(135deg, #2d3748 0%, #1a202c 100%);
      margin-top: 4rem;
      padding: 2rem 0 1rem 0;
      box-shadow: 0 -4px 20px rgba(0,0,0,0.1);
    }
    
    .social-links {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }
    
    .social-links a {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 45px;
      height: 45px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 50%;
      color: white;
      font-size: 1.3rem;
      transition: all 0.3s ease;
      text-decoration: none;
    }
    
    .social-links a:hover {
      background: #FF6B35;
      transform: translateY(-3px);
      box-shadow: 0 5px 15px rgba(255, 107, 53, 0.4);
    }
  </style>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
